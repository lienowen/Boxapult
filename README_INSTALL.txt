Parcel Patrol project-aligned asset pack v1

Use:
1. Open the Boxapult / Parcel Patrol repository root.
2. Extract this ZIP into that directory.
3. Allow it to merge the assets/, src/, and Docs/ directories.
4. The only intentional overwrite is src/config/assetManifest.js.

Do not extract the ZIP into public/ or src/ alone. The ZIP paths already match the project layout.
